# 作业批改多智能体 · 实施规格

把本文放到空目录，交给实施 AI，按本文从零实现完整可运行项目。  
**只允许按本文施工。不得沿用、拷贝、参考任何旧作业批改 Demo / JSON 文件记忆方案。不得自行改技术栈。**

---

## 0. 给实施 AI 的硬约束

1. 先通读全文，再写代码。未读完禁止开工。
2. 四个业务 Agent **同级**，不是流水线从属。编排器不是第五个业务 Agent。
3. Agent 之间只用 **A2A**。调工具只用 **MCP**。禁止 Agent 直接 import 另一个 Agent 的内部函数当「协作」。
4. 账本只用 **MySQL**。短记忆 / 流式 / 进行中任务只用 **Redis**。向量只用 **FAISS**。禁止 Milvus、MongoDB、SQLite、Neo4j。
5. 扫描件、空白卡、DOCX、PDF 只放文件系统，库里只存路径。禁止 BLOB。
6. 班级名单是硬门禁：未导入名单，只允许闲聊，禁止出卡、批改、按人学情。
7. 出卡 **不调外部 API / 不调 LLM**。客观题只认像素 OMR。主观题 LLM 只给建议分，低把握进老师确认队列，禁止当官方分写入教务。
8. 画像只允许知识点「会 / 弱 / 未测」。禁止性格、家庭、差生、心理、预测高考分。
9. 必须有自动化测试。没有测试的模块视为未完成。
10. 完成标准见第 16 节。未达到禁止宣称「做完了」。

---

## 1. 产品

面向高中单科老师的作业批改与教学闭环副驾驶。设计按约 50 人教学班。下面是教学上常见的使用顺序，**不是系统自动跑的流水线**；老师随时可通过对话只叫其中任意一个同级 Agent：

```
导入班级名单
  → 生成或导入试卷
  → 生成本地可打印答题卡并发给学生
  → 收回扫描
  → 客观题 OMR 像素批改 + 主观题切块 OCR + 大模型建议分 + 老师确认
  → 个人/班级学情（短、长期，可视化）
  → 备课（下次重点、学期规划）
  → 可 A2A 回推下一份作业
```

老师自然语言进入网关：普通聊天由大模型流式直出；业务任务由 A2A 编排器按点名派发。

### 1.1 做

- XLSX / CSV 一键导入班级名单
- 按科目、知识点生成试卷，或老师导入；接入 edupaper-mcp（Markdown / LaTeX / DOCX）
- 按本次题目结构定制答题卡（预印学号），本地生成，可打印
- 回收后：作业 ID 匹配模板、读学号、客观题 OMR、主观题切框 OCR
- 双涂、空卡、学号无效、OCR 乱码、给分把握低 → 老师确认队列
- 班级知识点正确率、需讲评题、连续薄弱学生、每人错题本
- 短长期统计可视化；备课提纲与学期规划
- 记忆（工作 / 作业情景 / 长期）与流式输出

### 1.2 不做

- 潦草练习本、模糊拍照当主输入（主路径是答题卡）
- 无标准答案仍机改客观题；无评分点把主观题打成精确官方分
- 写入学校教务成绩
- 学生人格标签、心理诊断、家庭评价
- 自动发家长群、钉钉 / 企微
- 全学科全家桶、自适应 APP、知识图谱中台
- 为「显得完整」而堆未调用的微服务

---

## 2. 架构（必须按此实现）

```
老师
  │
教师网关  Teacher Gateway
  ├ 流式 SSE（聊天 token + 长任务进度）
  ├ 会话记忆（Redis，回合结束后落 MySQL messages）
  └ 意图分类：chat | task | pipeline
        │
        ├ chat → 大模型直接流式回答（可带班级上下文，禁止编造成绩）
        │
        └ task / pipeline
              │
        硬门禁 名册服务     无名单且非 chat → 拒绝业务，提示先导入名单
              │
        A2A 编排器  Orchestrator
          不持有出题/出卡/学情/备课逻辑
          只做：发现 Agent Card、派发、串行/并行、写 task 状态
              │
        共享产物总线  homework_id + class_id
          MySQL 账本 + Redis 热状态 + 文件（图/DOCX）+ FAISS（向量）
              │
        ┌─────┴──────┬──────────┬──────────┐
        │            │          │          │
   作业生成 Agent  答题卡与批改 Agent  学情 Agent  备课 Agent
   （同级）        （同级）           （同级）     （同级）
        │            │          │          │
       MCP          MCP        MCP        MCP
   edupaper-mcp   出卡/OMR/OCR  统计图表   课标/知识点
                  评分 LLM
```

闭环允许：备课 Agent 经 A2A 把「下一份作业建议」发给作业生成 Agent。这是同级调用，不是从属。

老师可随时只叫任意一个同级 Agent（只出卡、只看学情、只导入试卷、只备课）。`task` 永远只派这一个。`pipeline` 仅当一句话里明确点了多个 Agent（如「出这份作业并出卡」）才按点名顺序派发，**禁止**自动跑完 1→2→3→4。

---

## 3. 技术栈（锁定）

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.11+ | 唯一语言 |
| Web / 流式 | FastAPI + SSE | 网关、MCP HTTP、A2A HTTP 可同端口 |
| 账本 | MySQL 8，InnoDB，utf8mb4 | SQLAlchemy 2.x |
| 热数据 | Redis 7 | 会话、Pub/Sub 或 Stream、A2A 进行中任务、名册门禁缓存 |
| 向量 | faiss-cpu + numpy | 与应用同进程；index 文件落盘 |
| 名册 | pandas + openpyxl + csv | XLSX / CSV |
| 图像 | Pillow + numpy | 出卡、OMR 像素、切块 |
| 文档 | python-docx | 导入试卷；edupaper-mcp 出 DOCX |
| LLM | OpenAI 兼容 HTTP（可配 DashScope / 其他） | 聊天、主观题建议分、出题文案 |
| OCR | 可插拔：本地或 HTTP OCR | 主观题切块后识别；客观题禁止走 OCR |
| 协议 | 自实现 MCP JSON-RPC + A2A JSON-RPC 子集 | 不必绑定易碎的官方 SDK 大版本 |
| 前端 | Streamlit 或 FastAPI+简单前端，二选一，默认 Streamlit | 老师操作台 |
| 部署 | docker-compose：mysql + redis | 应用本机跑 |
| 测试 | pytest | 必须 |

禁止：Django、Celery（Redis 任务状态够用）、Milvus、Elasticsearch、MinIO（本机文件即可）。

---

## 4. 仓库目录（必须按此建）

```
README.md
作业批改多智能体-实施规格.md    # 本文，保留
.env.example
.gitignore
docker-compose.yml
pyproject.toml
requirements.txt
pytest.ini
sql/schema.sql
samples/roster.example.csv
samples/paper.example.md          # 最小示例试卷，供导入测试
src/zypg/
  __init__.py
  __main__.py
  config.py
  models.py                       # pydantic：Artifact、Intent、A2AMessage
  gateway/
    app.py                        # FastAPI
    intent.py                     # chat / task / pipeline
    stream.py                     # SSE
  roster/
    service.py                    # 导入、硬门禁
  orchestrator/
    engine.py                     # A2A 派发
    cards.py                      # 四个 Agent Card
  agents/
    base.py                       # PeerAgent
    homework.py
    cards.py
    insight.py
    lesson.py
  mcp_tools/
    edupaper.py                   # 调 edupaper-mcp 客户端
    card_render.py                # 本地出卡，零外部 API
    omr.py
    ocr.py
    score_llm.py
    charts.py
  storage/
    mysql.py
    redis_store.py
    faiss_index.py
    files.py                      # 统一文件根目录
  protocol/
    jsonrpc.py
    mcp.py
    a2a.py
    catalog.py                    # 对外工具表唯一来源
  llm.py
tests/
  test_roster_gate.py
  test_intent.py
  test_a2a_peer.py
  test_omr_pixels.py
  test_grade_queue.py
  test_insight.py
  test_mysql_schema.py
```

包名 `zypg`。入口 `python -m zypg`。

---

## 5. MySQL 账本

`sql/schema.sql` 必须可被 MySQL 8 docker 初始化执行。核心表：

- `classes(class_id, name, grade, subject)`
- `students(student_id, student_no, name)`，`student_no` 唯一
- `enrollments(class_id, student_id)` 唯一
- `assignments(homework_id, class_id, name, subject, knowledge_points JSON, paper_path, extra JSON)`
- `items(homework_id, item_id, kind ENUM('objective','subjective'), number, stem, options JSON, answer_key JSON, rubric JSON, knowledge_point, score)`
- `card_templates(homework_id UNIQUE, geometry JSON, print_path)`
- `scans(homework_id, student_id NULL, file_path, status)`
- `item_results(homework_id, student_id, item_id, raw JSON, score, is_correct, source ENUM('omr','ocr_llm','teacher'), pending)`，三列唯一
- `review_queue(homework_id, student_id, item_id, reason, suggested_score, status ENUM('open','accepted','overridden'))`
- `conversations(conversation_id, homework_id NULL, class_id NULL)`
- `messages(conversation_id, role, content, created_at)`
- `insight_snapshots(homework_id NULL, class_id, scope ENUM('student','class'), window ENUM('short','long'), student_id NULL, payload JSON)`
- `mastery_events(student_id, homework_id, knowledge_point, mastery ENUM('mastered','weak','untested'))`
- `a2a_tasks(task_id, homework_id NULL, skill, status, payload JSON, result JSON)`
- `embedding_meta(faiss_id UNIQUE, kind ENUM('item','wrong','knowledge'), ref_id, homework_id NULL, text_hash)`

约定：

- 四个 Agent 共用这一份库，禁止各开各的库。
- 关联键永远是 `homework_id` + `class_id`（+ `student_id` / `item_id`）。
- 批完一场 50 张卡：OMR/OCR 结果与队列在 **同一事务** 提交。
- 老师确认后才把主观题 `source` 改为 `teacher`，`pending=0`。确认前学情可展示但须标注「含待确认」。

---

## 6. Redis

| 键 | 结构 | TTL | 用途 |
|---|---|---|---|
| `session:{sid}` | JSON | 小时 | 当前轮：intent、class_id、homework_id |
| `stream:{sid}` | Pub/Sub 或 Stream | 任务结束即丢 | token 与进度事件 |
| `task:{a2a_id}` | Hash | 天 | 进行中 A2A；完成后归档 MySQL |
| `roster:gate:{class_id}` | `1`/`0` | 随名册更新 | 硬门禁缓存 |
| `insight:hot:{class_id}` | JSON | 分钟 | 看板短统计 |

禁止用 Redis 存成绩事实、名单真相、扫描件。

流式：聊天每个 token 推 SSE；批改推 `{done: n, total: 50, student_id}`。禁止把长任务做成一次 HTTP 堵死无进度。

---

## 7. FAISS

- 应用内 `IndexFlatIP`（或 IVF，但默认 Flat）。向量 `float32`，检索前 L2 normalize。
- 索引文件 `data/indexes/items.faiss`。`faiss_id` 与 MySQL `embedding_meta` 对齐。
- 只服务三件事：
  1. 作业生成：历史小题近邻，避免重复、补同类
  2. 学情：错题题干近邻，聚到知识点
  3. 备课：薄弱知识点近邻，取相关题
- 写入时机：题目入库、错题确认后、知识点表更新。
- Embedding 模型可配置；测试允许用固定伪向量，不必下载大模型。
- 不上 Milvus。接口做成 `VectorIndex` protocol，便于以后替换，但默认实现只有 FAISS。

---

## 8. 名册硬门禁

- 支持 `.xlsx` / `.csv`。必需列：`student_no`, `name`。UTF-8-BOM 可。
- `POST /roster/import` 一键生成该班名单，返回 `class_id` 与人数。
- 无该班 `enrollments`：除 `intent=chat` 外全部拒绝，文案明确「请先导入班级名单」。
- 出卡必须按名单预印学号，一人一卡。禁止生成「无名卡」当正式发放。
- 示例文件：`samples/roster.example.csv`。

---

## 9. 意图分类

三分类，禁止只做「是否聊天」二分类。

| Intent | 例子 | 行为 |
|---|---|---|
| `chat` | 「什么是等差数列」 | LLM 流式直出，可引用班级上下文，不调四个 Agent |
| `task` | 「只出卡」「看看这班学情」「导入这份卷子」 | 编排器派给 **一个** Agent |
| `pipeline` | 「出这份作业并出卡」「改完并给出讲评」 | 仅按这句话点到的多个 Agent 派发，不自动补齐 1→2→3→4 |

实现：规则关键词 + 可选 LLM 分类。规则能定的不要花一次 LLM。测试必须覆盖上表三类。

---

## 10. 四个同级 Agent

每个 Agent：

- 实现 `PeerAgent`：`name`、`skills()`、`handle(A2AMessage) -> dict`
- 发布 A2A Agent Card（技能列表与 `skills()` 一致）
- 只通过 MCP 调工具；只通过 A2A 找其他 Agent
- 读写共享产物，禁止私有文件格式当唯一真相

### 10.1 作业生成 Agent

技能：`generate_paper`、`import_paper`

- 输入：`class_id`、科目、知识点、题型数量；或老师上传的 md/docx
- 生成路径：大模型出题结构 → MCP `edupaper-mcp` 排版为 Markdown/LaTeX/DOCX
- 导入路径：解析文件写入 `assignments` + `items`（含答案、评分点、知识点）
- 输出：`HomeworkArtifact`（homework_id、items、paper_path）
- 缺答案/评分点：允许保存试卷，但标记 `gradeable=false`，出卡可做，机改客观/主观要追问

edupaper-mcp：试卷生成 MCP（Markdown、LaTeX、DOCX）。进程内用 MCP 客户端调用，当作外部工具，不要把排版逻辑复制进 Agent。若本地拉不起该 MCP，允许用 **兼容同一工具名的 mock server** 跑测试，但生产配置指向真 MCP。

### 10.2 答题卡与批改 Agent（一个 Agent，两个 skill）

技能：`generate_cards`、`grade_scans`；另对外可有 `review_queue` 确认

**generate_cards（禁止调 LLM / 禁止外部 API）**

- 读 `items` 与名单，生成本地 PNG（或 PDF）答题卡 + `card_templates.geometry`
- 卡上必须有：作业 ID（条码或可读 CRC）、学号涂点或预印学号、客观题气泡、主观题作答框
- geometry 必须含每个气泡、学号区、主观框的像素坐标，供 OMR/切块使用
- 测试：给定固定题目与名单，几何可复现（种子或无随机）

**grade_scans**

1. 扫描件匹配 `homework_id` 模板  
2. 读学号；无效 → 队列 `reason=bad_id`  
3. 客观题：只根据气泡像素相对空白卡的填涂判定，对照 `answer_key`。禁止用 OCR/LLM 认选项  
4. 双涂、空涂 → 队列  
5. 主观题：按框切图 → OCR → 与标准答案/评分点比 → LLM **建议分** + 把握度  
6. 把握低、OCR 乱码 → `review_queue`，`pending=1`  
7. 客观题对错以像素为准；任何「标准答案 JSON 对一下就出分但不读图」的捷径视为作弊，测试必须抓

老师确认：`accepted` 采用建议分；`overridden` 写老师分。未确认不得当最终分进入长期画像 commit。

### 10.3 学情 Agent

技能：`class_insight`、`student_insight`、`commit_long_term`

- 两个角度：每个学生独立错题本；班级知识点正确率、均分、缺卡、连续薄弱
- 两套窗口：`short`=本作业；`long`=`mastery_events` 跨作业
- 可视化为一等产出：知识点正确率柱、掌握热力、得分分布。前端必须能画出，不能只有一段文字
- 正确率低于 60% 的知识点列入「下次必讲」
- `InsightPack` 写入 `insight_snapshots`，备课只读这个，禁止备课去扫 `scans` 原图
- commit 长期：把本场已确认结果追加 `mastery_events`（会/弱/未测）

### 10.4 备课 Agent

技能：`next_focus`、`term_plan`、`suggest_next_homework`

- 只读 InsightPack + 长期 mastery
- 输出：下次讲课重点、该讲哪些人、学期规划要点
- `suggest_next_homework`：A2A 发给作业生成 Agent，带知识点列表。不直接改试卷表，由作业生成 Agent 写 Artifact

---

## 11. A2A 与 MCP

### 11.1 A2A

- `GET /.well-known/agent-card.json` 每 Agent 一份（或编排器聚合四份 skills）
- `POST /a2a` JSON-RPC：`message/send`、`tasks/get`、`tasks/cancel`
- 任务状态：`submitted | working | completed | failed | canceled`
- 进行中写 Redis `task:`，结束写 MySQL `a2a_tasks`
- 四个 Agent 互调必须走 HTTP A2A（测试可用 ASGI 内存客户端），禁止 `HomeworkAgent().handle()` 被 CardsAgent 直接调用（测试可 spy）

### 11.2 MCP

`protocol/catalog.py` 是 **唯一** 对外工具表。至少包含：

- `ask`（老师自然语言，走网关）
- `import_roster`
- `generate_paper` / `import_paper`
- `generate_cards`
- `grade_scans`
- `show_insight`
- `accept_llm_scores`
- `plan_lesson`
- `a2a_send`

MCP：`initialize`、`tools/list`、`tools/call`。stdio + HTTP `POST /mcp` 至少实现 HTTP。

---

## 12. 网关 HTTP（最小）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | mysql + redis ping |
| POST | `/roster/import` | 上传 xlsx/csv |
| POST | `/ask` | 非流式问 |
| GET | `/ask/stream` | SSE |
| POST | `/a2a` | JSON-RPC |
| POST | `/mcp` | JSON-RPC |
| GET | `/.well-known/agent-card.json` | 编排器卡 |
| GET | `/insights/{class_id}` | 短长期快照 |

鉴权：本机绑定 `127.0.0.1`。不要对公网开放默认配置。

---

## 13. 老师界面（必须能点）

Streamlit 即可，页面：

1. 导入名单  
2. 对话（流式）  
3. 出题 / 导入试卷  
4. 出卡预览与「打印文件」路径  
5. 上传扫描件并批改进度  
6. 确认队列  
7. 学情图（班级 + 个人）  
8. 备课提纲  

无名单时业务按钮禁用或点了给出与 API 相同的拒绝文案。

---

## 14. LLM 与安全

- 无 Key 时：聊天说明未配置；出题可走「仅导入」；主观题无 LLM 则全部进确认队列，禁止随机打分
- 送入 LLM 的主观题图/文本：去掉姓名学号，只用匿名 `student_id`
- 系统提示禁止输出人格贬义、家庭评价、高考预测
- 客观题路径代码里不得出现 LLM 调用

---

## 15. 实现顺序（按阶段交付，每阶段有退出条件）

### 阶段 A 骨架

docker-compose 起 MySQL/Redis；schema 导入；`GET /health` 真 ping 通过。

### 阶段 B 名册 + 意图 + 聊天

导入 CSV/XLSX；门禁单测；chat 流式（无 Key 则回固定「未配置」流，但 SSE 协议要真）。

### 阶段 C 作业生成

导入试卷进 MySQL items；generate 可 mock edupaper-mcp 仍写出 homework_id。

### 阶段 D 出卡

本地渲染卡 + geometry；按名单 1 人 1 张；禁止网络。

### 阶段 E 批改

合成填涂图（测试夹具）→ OMR 与夹具答案一致；主观切块进队列；确认后写 item_results。

### 阶段 F 学情 + 备课

快照、图、60% 必讲、长期 mastery；备课读 InsightPack；A2A 回推建议。

### 阶段 G 协议与打磨

MCP tools/list 与 catalog 一致；A2A 四 Agent 互调；README 可按步骤跑通最小闭环（可用 3 人名单 + 2 客观 1 主观）。

每阶段合并前：该阶段测试全绿。禁止先堆四个空 Agent 再声称架构完成。

---

## 16. 质量保证（完成标准）

实施 AI 在最终回复中必须附上 pytest 结果。下列全部通过才算完成。

### 16.1 必须存在的测试

| 测试 | 断言 |
|---|---|
| `test_roster_gate` | 无名单一出卡/批改被拒；导入后放行 |
| `test_intent` | chat / task / pipeline 三例分类正确 |
| `test_a2a_peer` | 四 Agent skills 非空且同级；Cards 不直接 import Homework 内部私有函数完成出题 |
| `test_omr_pixels` | 夹具图上涂 A，OMR 得 A；改像素不改 JSON 则结果跟着像素变 |
| `test_grade_queue` | 低把握主观题 pending，确认后才非 pending |
| `test_insight` | 班级+学生两视角；短长期字段存在；画像无违禁词 |
| `test_mysql_schema` | 关键表可插入 homework_id 关联 |

### 16.2 禁止的偷懒

- 用标准答案 JSON 对学号直接出客观分，不读图
- 四个 Agent 写成四个函数在一个文件里互相调用，却宣称 A2A
- Redis 当成绩库
- 无名单仍出正式卡
- 主观题 LLM 分直接当最终分 commit 长期
- README 截图式空壳、按钮点了 NotImplemented

### 16.3 README 必须能让陌生人做的事

1. `docker compose up -d`  
2. 配 `.env`  
3. `pip install -r requirements.txt` 且 `pip install -e .`  
4. `python -m zypg`  
5. 导入 `samples/roster.example.csv`  
6. 导入或生成一份最小卷  
7. 出卡  
8. 用测试夹具当扫描件批改  
9. 打开学情图  

缺任一步视为未完成。

---

## 17. 配置

`.env.example` 必须含：

```
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=zypg
MYSQL_PASSWORD=zypg
MYSQL_DATABASE=zypg
REDIS_URL=redis://127.0.0.1:6379/0
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
ZYPG_HOST=127.0.0.1
ZYPG_PORT=8765
EDUPAPER_MCP_MOCK=0
EDUPAPER_MCP_CMD=
EDUPAPER_MCP_ARGS=-y,--no-update-notifier,edupaper-mcp
EDUPAPER_MCP_URL=
OCR_URL=
```

docker-compose：`mysql:8` 挂载 `sql/` 为 init；`redis:7`。应用不进镜像也行，但 compose 必须能起依赖。

---

## 18. 给实施 AI 的开工指令（复制到对话第一条）

```
你是实施工程师。唯一需求文档是《作业批改多智能体-实施规格.md》。
在当前空目录从零实现。遵守文中硬约束、目录、栈、表结构、测试清单。
按阶段 A→G 推进，每阶段先写测试再写实现。
不要引入规格外的库和产品功能。
全部 pytest 通过后，用 README 的步骤自检最小闭环，再提交。
最终回复列出：目录树、如何启动、pytest 输出摘要、已知限制。
```

---

## 19. 最小演示数据规格

- 名单 3 人：`S01 张一` `S02 李二` `S03 王三`
- 试卷：客观 2 题（四选一，答案 A、C），主观 1 题（满分 10，含 2 条评分点），知识点各一
- 出卡 3 张；测试夹具：S01 全对、S02 客观全错主观低把握、S03 缺学号
- 期望：S03 进队列；S02 主观 pending；班级图里能看到该知识点正确率；备课能读到「必讲」或相对最弱知识点

做完以这套数据为验收剧本，不要用空库截图充数。
